- clean: []
- shell:
    - mkdir -p ~/.shellrc
    - mkdir -p ~/Downloads/Torrents
    - mkdir -p ~/Downloads/Chrome
    - mkdir -p ~/Downloads/Firefox
    - mkdir -p ~/Downloads/Personal/Development
    - mkdir -p ~/Downloads/Professional
    - mkdir -p ~/Dropbox
    - mkdir -p ~/Google\ Drive
    - mkdir -p ~/.ssh
    - cp -rn homeConfigFiles/.config ~/.config
    - cp homeConfigFiles/bashrc ~/.bashrc
    - cp homeConfigFiles/bash_profile ~/.bash_profile
    - cp -rn homeConfigFiles/.ssh ~/.ssh
- link:
    ~/.shellrc/bashrc.d: 
        create: true
        path: bashrc.d/
    ~/.shellrc/bin:
        create: true
        path: bin
    ~/.vimrc: homeConfigFiles/vimrc
